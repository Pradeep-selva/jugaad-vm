#include<stack>
#include<string>
#include<vector>

#ifndef TRAVERSE_STACK_H
#define TRAVERSE_STACK_H

void traverse_stack(std::stack<int> & st);

#endif

#ifndef TOKENIZE_H
#define TOKENIZE_H

void tokenize(std::string const &str, std::vector<std::string> &out);

#endif